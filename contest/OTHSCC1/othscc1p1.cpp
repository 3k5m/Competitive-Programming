#include <bits/stdc++.h>
using namespace std;

void solve(){
    int a, b;
    cin>>a>>b;
    if(!a || !b){
        cout<<0;
    }else{
        cout<<a+b;
    }
}

int main() {

    ios_base::sync_with_stdio(0);
    cin.tie(0)->sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    solve();

    return 0;
}